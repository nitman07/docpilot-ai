apiVersion: v1
kind: ConfigMap
metadata:
  name: docpilot-config
  namespace: {{ .Values.global.namespace }}
data:
{{- range $key, $value := .Values.api.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}